<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.projet">

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="DATABASE" android:value="sugar_example.db" />
        <meta-data android:name="VERSION" android:value="3" />
        <meta-data android:name="QUERY_LOG" android:value="true" />
        <meta-data android:name="DOMAIN_PACKAGE_NAME" android:value="app.projet" />

        <activity
            android:name=".MainActivity"
            android:label="Connexion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ListeDisciplines"
            android:label="Disciplines" />
        <activity
            android:name=".ExercicesMaths"
            android:label="Mathématiques" />
        <activity
            android:name=".MathsMultiplications"
            android:label="Multiplications" />
        <activity
            android:name=".ExercicesCultureG"
            android:label="Culture générale" />
        <activity
            android:name=".MathsMultiplicationOpt"
            android:label="Multiplications" />
        <activity android:name=".MathsAdditions"></activity>
    </application>

</manifest>